import React from 'react';
import ProductList from './ProductList';

const Invoice = () => {
    return (
        <div>
            <h2>Invoice</h2>
            <ProductList />
        </div>
    );
};

export default Invoice;
